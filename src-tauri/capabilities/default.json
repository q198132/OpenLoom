{
  "identifier": "default",
  "description": "Default capabilities for OpenLoom",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
